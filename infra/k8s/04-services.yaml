apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: loja-veloz
spec:
  type: ClusterIP
  selector:
    app: orders
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
